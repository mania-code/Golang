package main

import "fmt"

func main() {
	var name string = "golang"
	var isAduit = true
	age := 21

	fmt.Println(name)
	fmt.Println(isAduit)
	fmt.Println(age)

	// we need to define type(string, int, bool etc) when we want to predefine variable so assign it's value later

	var group string
	group = "DevOps"
	fmt.Println(group)
}
